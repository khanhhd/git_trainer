<% provide(:title, 'All users') %>
<h1>All users</h1>
<!-- Test commit 
	Author : DaoLM
	Date : Friday, 18 October -->
<ul class="users">
  <% @users.each do |user| %>
    <%= render "users/user", user: user %>
  <% end %>
</ul>
<%= will_paginate %>
